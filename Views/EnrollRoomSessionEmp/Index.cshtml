@model IEnumerable<santisart_app.Models.Enroll_RoomSession_Emp>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            ชื่อ - สกุล
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RoomSession.RoomName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.staft)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.value)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.timestamp)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Employee.EmpTitle) @Html.DisplayFor(modelItem => item.Employee.EmpName) @Html.DisplayFor(modelItem => item.Employee.EmpLname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RoomSession.RoomName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.staft)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.value)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.timestamp)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EnrollRoomEmpId }) |
            @Html.ActionLink("Details", "Details", new { id=item.EnrollRoomEmpId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EnrollRoomEmpId })
        </td>
    </tr>
}

</table>
