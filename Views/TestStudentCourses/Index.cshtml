@model IEnumerable<santisart_app.Models.TestStudentCourse>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TestCourse.CourseName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TestStudent.FirstName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TestCourse.CourseName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TestStudent.FirstName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StudentCourseId }) |
            @Html.ActionLink("Details", "Details", new { id=item.StudentCourseId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StudentCourseId })
        </td>
    </tr>
}

</table>
